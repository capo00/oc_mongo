{
  "name": "oc_mongo",
  "version": "0.1.1",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm publish --access public"
  },
  "keywords": ["nodejs", "mongodb", "dao", "database", "mongo"],
  "author": "",
  "license": "MIT",
  "description": "NodeJS library with simple API for working with mongodb.",
  "dependencies": {
    "mongodb": "^6.8.0",
    "nodemon": "^3.1.4"
  }
}
